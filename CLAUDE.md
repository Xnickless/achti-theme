# Achti B2B — motyw Shopify (Trade + modyfikacje B2B)

Ten folder to repozytorium `Xnickless/achti-theme` dla sklepu Shopify `ccucsr-si.myshopify.com`.

**Wdrażanie (stan na 04.09.2026):** integracja GitHub → Shopify **nie działa** (sprawdzone: motyw live „Trade” #201190474070 to stockowy Trade z 8 lipca, żaden motyw nie dostaje pushy z `main`). Kod wdrażamy ręcznie do wersji roboczej:
```
shopify theme push --store ccucsr-si.myshopify.com --theme 206234878294 --allow-live   # „achti-trade-b2b-v4”, od 07.09.2026 LIVE (flaga --allow-live wymagana)
```
**07.09.2026: motyw #206234878294 opublikowany jako live** (stary Trade #201190474070 został jako wersja robocza). Push do tego samego ID aktualizuje teraz motyw live. Strona /pages/rejestracja ma przypisany szablon „rejestracja”. Jeśli klient edytuje motyw w „Dostosuj”, zmiany są w Shopify, nie w repo — przed pracą `shopify theme pull --theme 206234878294 --only "templates/*.json" --only "sections/*-group.json" --only config/settings_data.json` i porównaj.

## Kontekst projektu
- Klient: Adrian (marka Achti, polski producent czapek, kominów, kominiarek). Platforma hurtowa B2B: ceny tylko dla zaakceptowanych firm, rejestracja po NIP/VAT UE z ręczną akceptacją.
- Języki: PL (domyślny), EN, DE, FR. Termin startu: koniec września 2026.
- Baza: oficjalny motyw Shopify **Trade** (architektura Dawn). Nie przepisujemy motywu — dokładamy minimalne, punktowe zmiany.
- Decyzje klienta (dokument „PLATFORMA ACHTI B2B”, Google Docs, 07.09.2026):
  - **Dostępność:** cała oferta „Na zamówienie” bez stanów; wybrane modele oznaczane „Dostępny od ręki” (włączone śledzenie stanów + ilość > 0). Zrobione w bloku inventory main-product (stany: on_order / in_stock / unavailable, klucze `b2b.availability.*`, dopisek czasu realizacji w ustawieniach bloku).
  - **Kolory = warianty** z własnymi stanami; nazwy kolorów numerami lub zestawami (np. „120 - Czarny”, „SET 5”). Swatche z nazw nie zadziałają — docelowo zdjęcia wariantów jako miniatury.
  - **Rozmiary:** One Size; dzieci 50-52 i 52-54 (jako warianty rozmiaru w dziecięcych).
  - **Katalog PDF:** dynamiczny z cenami klienta tylko jeśli ceny per klient (Shopify: katalogi B2B per firma — możliwe); inaczej bez cen.
  - **Polityka cenowa:** kilka progów zależnych od ilości sztuk lub kwoty (Shopify B2B: progi ilościowe w katalogu; motyw Trade pokazuje „Ceny zależne od wolumenu”).
  - Nowe punkty z dokumentu: zdjęcia każdej czapki na modelce, waga brutto w CSV/produkcie, integracja z kurierem, Klaviyo, XML z ofertą (zrobiony: `/collections/all?view=feed`).

## Co już jest dodane (nie duplikuj)
- Ukrywanie cen/zakupu dla gości: ustawienia `settings.b2b_hide_prices`, `settings.b2b_require_company`, `settings.b2b_register_page` (config/settings_schema.json, grupa „B2B — dostęp do cen”).
- Wzorzec dostępu (wklejany na początku plików, które go potrzebują):
  ```liquid
  {%- liquid
    assign b2b_access = true
    if settings.b2b_hide_prices
      if customer == nil
        assign b2b_access = false
      elsif settings.b2b_require_company and customer.b2b? != true
        assign b2b_access = false
      endif
    endif
  -%}
  ```
  Użyty w: snippets/price.liquid, snippets/buy-buttons.liquid, snippets/card-product.liquid, sections/main-product.liquid (bloki inventory, quantity_selector, text z „VAT”), sections/quick-order-list.liquid, sections/bulk-quick-order-list.liquid.
- `snippets/b2b-gate.liquid` — komunikat zamiast ceny (`style: 'inline'`) lub panel z przyciskami (`style: 'box'`). Koszyk (main-cart-items, main-cart-footer, cart-drawer) pokazuje gościom bramę zamiast pozycji i cen (07.09.2026). Filtr „Dostępność” ukryty w snippets/facets.liquid (cała oferta jest „na zamówienie”). Formularz kontaktowy ma pole „Nazwa firmy” (`templates.contact.form.company`).
- **Konfigurator czapek z logo (07.09.2026, od 13.09.2026 pod „Czapki reklamowe”):** na życzenie Kamila przeniesiony z kolekcji `private-label` na `czapki-reklamowe` — szablon `templates/collection.czapki-reklamowe.json` (dawniej `collection.private-label.json`; stary plik nadal leży na motywie live jako nieużywany), templateSuffix `czapki-reklamowe` ustawiony przez API, 8 modeli demo przeniesionych między kolekcjami (collectionAddProducts/RemoveProducts), eyebrow sekcji „Czapki reklamowe”, opis kolekcji PL + EN/FR/DE (`COLLECTIONS` w translate_catalog.py, `set_translations --only=collections,theme`). Kolekcja `private-label` jest teraz zwykłą, pustą kolekcją bez szablonu (opis o produkcji pod marką klienta został) — do decyzji, czy zostaje w menu. Nazwy plików sekcji/JS/CSS (`private-label-configurator.liquid`, `private-label.js`, `.pl-*`, klucze `b2b.pl.*`) bez zmian. Sekcja `sections/private-label-configurator.liquid` + `assets/private-label.js` + CSS `.pl-*` w b2b.css, klucze `b2b.pl.*` w 4 locales. Kroki: model (z kolekcji) → kolory z ilościami (metapole `inne_kolory`) → znakowanie (bloki sekcji) + miejsce → logo z podglądem na canvas (drag, skala, obrót, pobranie PNG) → dane firmy. Podgląd „mądry” (07.09.2026): `plImg.analyzeHat` wykrywa obrys czapki (pozycje i wielkość logo liczone względem czapki), `plRenderScene` wtapia logo w dzianinę (mnożenie przez jasność, odkształcenie po splocie, krzywizna walca), tekstura ściegów gdy znakowanie zawiera „haft”, `cleanLogo` usuwa białe tło z JPG, warianty biały/czarny + ostrzeżenie o kontraście, pasek podglądów na wszystkich wybranych kolorach i „Pobierz wszystkie kolory”. Zdjęcia z CDN Shopify mają CORS, więc canvas nie jest „tainted”. Wysyłka = formularz kontaktowy Shopify (`contact[Konfiguracja]`, `contact[Podgląd (obraz data URL)]` 360×450 JPEG). Nie da się przetestować wysyłki na dev serwerze (POST /contact → 401 za hasłem sklepu) — test na live.
- `docs/strony/` — projekty regulaminu B2B, polityki prywatności, cookies, wysyłki, zwrotów, FAQ i warunków współpracy + README z decyzjami klienta. **Wgrane do sklepu 07.09.2026** przez panel (polityki: privacy/terms/shipping/refund; strony warunki-wspolpracy, faq, polityka-cookies; menu obsluga-klienta + Regulamin B2B, Polityka cookies). Stopka pokazuje linki polityk automatycznie (`shop.policies`). Pola `[…]` w treści do uzupełnienia przez klienta w panelu. Podgląd: https://claude.ai/code/artifact/dfddfe42-5561-49c0-9abb-642afa7c33ba
- `sections/b2b-register.liquid` + `templates/page.rejestracja.json` — formularz rejestracji firmy (contact form, walidacja NIP w JS).
- `assets/b2b.css` — style B2B, przekreślone niedostępne swatche, „load more”. Style nagłówka są w `{% style %}` w sections/header.liquid.
- Typografia (10.09.2026): `assets/b2b-typo.js` (w layout/theme.liquid) wstawia twardą spację po jednoliterowych spójnikach/przyimkach (a, i, o, u, w, z) w treści strony, także w treści dogrywanej (MutationObserver); opisy kolekcji, produktów i stron są justowane z `hyphens: auto` od 750 px (b2b.css). Opisy 5 kolekcji (Ona, On, Dla dzieci, Premium, Private Label) skrócone 10.09.2026 do jednej linii (≤ 85 znaków, wyśrodkowane, bez justowania) na życzenie Kamila (PL przez API, EN/FR/DE w `COLLECTIONS` w translate_catalog.py + `set_translations.py … --only=collections`).
- Wyszukiwanie po kodzie (10.09.2026): Shopify dopasowuje SKU tylko od początku tokenu (samo „1145” = 0 wyników). `assets/b2b-search.js` zamienia zapytanie z samych cyfr / „az 1145” / „az1145pc” na „AZ-1145…” w podpowiedziach (hook w `getSearchResults` w assets/predictive-search.js) i przy wysłaniu formularza; wpięty w layout/theme.liquid po search-form.js.
- Nagłówek: `logo_position: top-left`, pole wyszukiwania inline (`section.settings.inline_search`), klasa `header--inline-search`. Od 10.09.2026 (wieczór): niski nagłówek — logo 180 px po lewej, menu pośrodku i ikony po prawej w jednym wierszu, wyszukiwarka pod spodem (kolumny `1fr auto 1fr`, więc menu jest dokładnie na środku strony jak wyszukiwarka; nagłówek szerszy niż treść: max 156 rem i padding 3 rem, logo przy lewej, ikony przy prawej krawędzi; grid `'heading navigation icons' / 'inline-search…'` w {% style %} sections/header.liquid), padding sekcji 4/8 px (13.09.2026, wcześniej 12/16 — Kamil chciał niższą białą górę, żeby hero było optycznie większe; krok ustawienia to 4 px), row-gap między wierszem logo/menu a wyszukiwarką 0.4 rem (było 1.2), wyszukiwarka 4.2 rem (było 4.6) w bloku „Nagłówek na desktopie” w b2b.css, `sticky_header_type: always` — po przewinięciu zostaje pasek z małym logo po lewej, nazwami kategorii i ikonami konta i koszyka po prawej (wyszukiwarka ukryta regułami `.scrolled-past-header` w sections/header.liquid).
- Listing: 4 kolumny od 11.09.2026 rano (decyzja Kamila; 10.09 były 3); siatka i pasek filtrów w kolekcjach i wyszukiwarce mają 144 rem (padding 4 rem) zamiast 120 rem, odstępy kafelków 2.4/3.2 rem przez zmienne `--grid-desktop-*-spacing` (blok „Listing na 4 kolumny” w b2b.css; 156 rem było dla Kamila za szerokie, a sam `column-gap` łamie rząd do 3 kafelków, bo Trade liczy szerokość z tej zmiennej), szerokość strony 1200 px (`page_width`; 10.09 próbowano 1600 — Kamil wolał mniejsze kafelki, tylko nagłówek jest szerszy, 156 rem), ciągłe doładowywanie (skrypt na końcu sections/main-collection-product-grid.liquid). Czcionki od 10.09.2026: nagłówki Tenor Sans (`tenor_sans_n4`, wcześniej Fraunces), treść Jost.
- Karta produktu (templates/product.json): title, sku, price, tekst „Cena netto — VAT naliczany w koszyku”, variant_picker (swatche), inventory, quantity, buy_buttons, description (blok ma ustawienia `collapsible`/`show_intro`/`heading`/`icon`: pierwszy akapit widoczny, reszta w rozwijanej sekcji „Opis i specyfikacja”), zakładki Rozmiar/Skład/Kolor z metapól `custom.rozmiar`, `custom.sklad`, `custom.kolory`.
- Tłumaczenia B2B: klucze `b2b.*` w locales/pl.json, en.default.json, de.json, fr.json.
- **Producent (GPSR):** ustawienia motywu `producer_name/address/email/phone` (grupa „Producent (GPSR)”), blok `producer` w main-product (w templates/product.json na końcu) i `<manufacturer>` w feedzie XML. **Zgoda marketingowa:** checkbox wymagany (`contact[accepts_marketing]`) w newsletterze stopki (ustawienie `newsletter_consent`) i w popupie, tekst `newsletter.consent_html` w 4 locales (08.09.2026).
- Stopka bez „Powered by Shopify”. Jedna czcionka (DM Sans) w config/settings_data.json.

## Zasady pracy
- Przed commitem: `shopify theme check` (0 błędów) **i** ręczna kontrola, że żaden `{% if %}` nie przecina granic `{% when %}` w `case` — theme check tego nie wykrywa, a Shopify odrzuca cały motyw.
- Podgląd na żywo: `shopify theme dev --store ccucsr-si.myshopify.com` (loguje przez przeglądarkę, nie publikuje).
- Nie zmieniaj plików JSON w `templates/` i `sections/*-group.json`, jeśli klient mógł je edytować w edytorze — najpierw `git pull` i sprawdź diff.
- Teksty widoczne dla klienta: po polsku; nowe stringi zawsze dodawaj do wszystkich czterech plików locales.
- Commity po polsku, krótko, np. `Nagłówek: pole wyszukiwania w jednej linii`.

## Skonfigurowane w panelu 04.09.2026
- Strona `rejestracja` (tytuł „Rejestracja firmy”), bez szablonu — przypisać „rejestracja” po publikacji motywu.
- Kolekcje automatyczne po tagach produktu: Damska = `damskie`, Męska = `meskie`, Dla dzieci = `dzieci`, Premium = `premium`, Nowości = `nowosc`, Wyprzedaż = cena porównawcza > 0, Najpopularniejsze (`najpopularniejsze`, cena > 0, sortowanie „najlepiej sprzedające się”). Ręczne (klient dodaje produkty): Bestsellery, Czapki reklamowe, Private Label. Kolekcje kategorii sortują od najnowszych (CREATED_DESC), tag `nowosc` ma 12 najnowszych SKU (AZ-3090…AZ-3102PC). Klient przy dodawaniu produktu wpisuje tagi — bez tagu produkt nie pojawi się w żadnej kategorii menu.
- Menu główne podpięte pod kolekcje. Stopka (wg projektu klienta z docx „PLATFORMA ACHTI B2B”): kolumny Kolekcje = `main-menu`, Informacje = menu `informacje` (O nas, Produkcja, Materiały, Jakość, Zrównoważony rozwój, Blog, Kontakt — strony `/pages/o-nas`, `/pages/produkcja`, `/pages/materialy`, `/pages/jakosc`, `/pages/zrownowazony-rozwoj` **jeszcze nie istnieją**), Obsługa klienta = menu `obsluga-klienta` (Logowanie B2B, Rejestracja firmy, Warunki współpracy, Wysyłka i dostawa, Zwroty i reklamacje, FAQ, Regulamin B2B, Polityka cookies — wszystkie istnieją od 07.09.2026). Linki social (Instagram, Facebook, LinkedIn) do wpisania w Ustawienia motywu → Media społecznościowe.
- Popup newslettera na stronie głównej: sections/newsletter-popup.liquid (opóźnienie, localStorage na X dni, nie dla klientów z accepts_marketing; w edytorze otwiera się po zaznaczeniu sekcji).
- Feed XML produktów: `/collections/all?view=feed` (templates/collection.feed.liquid), ceny tylko gdy widoczne.
- W sklepie jest zainstalowana aplikacja „SP Hide Price & Access” — dubluje ukrywanie cen z motywu; do decyzji klienta, czy zostaje.

## Sprzedaż zagraniczna — konfiguracja panelu (08.09.2026)
- Rynki: Polska (PLN) + **Unia Europejska** (26 krajów, EUR, kurs dynamiczny, bez zaokrąglania). Selektor kraju i języka w stopce włączony.
- Wysyłka: strefy Domestic (PL), International (27 krajów, 69 zł), **Unia Europejska** (13 krajów UE brakujących w International, „Kurier (UE)” 69 zł, 3–5 dni) — stawki tymczasowe, klient podaje cennik kuriera.
- Podatki: Shopify Tax, VAT PL włączony (PL6812081859), „ceny zawierają podatek” **wyłączone** (ceny netto), OSS nie włączony. Checkout: nazwa firmy wymagana, numer VAT firmy opcjonalny → odwrotne obciążenie dla ważnych numerów VAT UE. Wymagane logowanie przed zakupem włączone.
- Języki: PL (domyślny) + **DE, EN, FR opublikowane** (08.09.2026). DE produkty = auto-tłumaczenie Shopify (Translate & Adapt); EN i FR produkty, kolekcje, menu i teksty motywu = tłumaczenia z szablonu (`tools/translate_catalog.py` → `tools/translations/*.json`) wgrane przez `tools/set_translations.py` (translationsRegister); DE menu i teksty motywu też ze skryptu (poprawka „Sie/An”). Aplikacja „Import Katalogu” ma scope read/write_translations, read/write_locales, read/write_content, read_online_store_navigation, read_themes. Tłumaczenia tekstów motywu są per motyw (wgrane na live #206234878294; dev theme ich nie ma). Polityki (prywatność, regulamin, wysyłka, zwroty) i strony (warunki współpracy, FAQ, cookies) przetłumaczone na EN/DE/FR 08.09.2026: źródła PL `tools/translations/pages-src/`, tłumaczenia `tools/translations/pages/<loc>/`, wgrywanie `tools/set_page_translations.py en de fr` (pomija dokument, jeśli treść PL w sklepie zmieniła się od czasu tłumaczenia — wtedy zaktualizować źródło i tłumaczenie). Po uzupełnieniu przez Adriana pól `[…]` w PL trzeba uzupełnić te same pola w 3 tłumaczeniach. Nieprzetłumaczone: metapola rozmiar/skład.
- Cookies: baner włączony (automatyczne regiony = rynki), dane w UE.
- **Podgląd ceny w innej walucie (11.09.2026):** przełącznik PLN / EUR / USD w nagłówku (`snippets/b2b-currency.liquid` renderowany w `.header__icons`, tylko gdy `b2b_access`), logika w `assets/b2b-currency.js`: kursy z api.frankfurter.app (EBC) cache w localStorage 12 h + awaryjne kursy zaszyte w pliku, przeliczanie tekstów cen w węzłach tekstowych (regex na kwotę + symbol, MutationObserver dla treści dogrywanej: koszyk, podpowiedzi, Section Rendering), wybór zapamiętany w localStorage, przy walucie innej niż waluta sklepu obok przycisku „Realizuj zakup” pojawia się dopisek o rozliczeniu w PLN. To tylko podgląd — koszyk, checkout i faktura idą w walucie rynku (PLN, dla rynku UE EUR). Teksty: `b2b.currency.label/note` w 4 locales. Prawdziwa waluta rozliczenia = rynek/katalog B2B (Rynki: Polska PLN, Unia Europejska EUR; USD wymagałby nowego rynku).

## BaseLinker (Base.com) — integracja 08.09.2026
Konto trialowe Adriana (Achti sp. z o.o. sp.k.), sklep podłączony przez aplikację „BaseLinker” ze Shopify App Store (Integracje → Shopify App „ccucsr-si”, ID 5032048). Skonfigurowane w panelu Base: pobieranie zamówień co 10 min do statusu „Nowe zamówienia”, VAT ze sklepu (nie z magazynu), bez dodawania zamówień do sklepu, bez korekty stanów, synchronizacja stanów i cen **wyłączona** (oferta „na zamówienie”), statusy Base → Shopify: Wysłane = Fulfilled, Anulowane = Cancelled, przekazywanie statusu płatności = Tak, numery nadania wysyłane przy statusie „Wysłane”. Zaawansowane: dane firmy B2B i NIP klienta do faktury, adres dostawy kopiowany do faktury gdy pusty, powiadomienia e-mail Shopify o statusie i trackingu włączone, język pl, domyślny VAT 23. **Do zrobienia (dane od Adriana):** konto kuriera (Integracje → kurierzy), moduł faktur (numeracja, dane firmy), szablon e-mail, ewentualnie magazyn w Base dla modeli „dostępny od ręki”. Test: złożyć zamówienie testowe w sklepie i sprawdzić w Base po 10 min.

## Test jako firma B2B (07.09.2026)
Firma testowa „Firma Testowa Achti” (klient Kamil Test, e-mail Kamila) — zatwierdzona, bez katalogu. Ceny, szybkie zamawianie, koszyk i checkout z danymi firmy działają. Do decyzji/konfiguracji klienta:
- **Podatki:** Shopify dopisuje „Z wliczonymi podatkami”, a motyw mówi „Cena netto”. Dla hurtu netto wyłączyć Ustawienia → Podatki i cła → „Uwzględnij podatek w cenach” i ustawić VAT 23% PL. Wtedy dopisek zniknie sam.
- **Płatności:** w checkout tylko PayPal. B2B potrzebuje przelewu/faktury: Ustawienia → Płatności → Ręczne metody płatności („Przelew bankowy”) i/lub warunki płatności per firma (Klienci → Firmy → Warunki płatności).
- **Katalog B2B:** Markets → Katalogi → katalog z cenami hurtowymi przypisany do firm; bez niego firmy widzą ceny domyślne.
- **Panel konta klienta** (po zalogowaniu) to hostowana strona Shopify: wygląd w Ustawienia → Konta klientów → Personalizacja; menu w Treść → Menu → „Menu główne konta klienta”.
- Sekcje z pustą kolekcją (Nowości, Bestsellery, cross-sell w koszyku) są ukryte w sklepie do czasu dodania produktów (widoczne tylko w edytorze). Zachęta do rejestracji i drugi przycisk hero są ukryte dla zalogowanych firm (ustawienia `hide_for_b2b` / `button_2_hide_b2b`).

## Katalog produktów (import 07.09.2026)
- 362 produkty zaimportowane skryptem `tools/shopify_import.py` (Admin API, poświadczenia aplikacji „Import Katalogu” z Dev Dashboard w `~/.config/achti/client_id` + `client_secret`, poza repo). Dane: `tools/achti-produkty.json` wygenerowane przez `tools/generate_catalog.py` z arkusza `~/Downloads/achti-katalog-do-uzupelnienia.csv` (z nazw zdjęć). Zdjęcia po obróbce (białe tło, 4:5): `~/Downloads/ACHTI COLLECTION 2026-2027 - biale tlo`.
- ~~Nazwy robocze (miasta) i ceny przykładowe G3–G12~~ — od 10.09.2026 w sklepie są nazwy handlowe i ceny netto Adriana (zob. „Arkusz od Adriana naniesiony”). Kolory wariantów nadal puste.
- `docs/instrukcja-katalog.md` — instrukcja dla klienta, jak uzupełnić arkusz (08.09.2026). `docs/instrukcja-import-klientow.html` + `docs/import-klientow-szablon.csv` (11.09.2026) — instrukcja dla Adriana, jak wgrać posiadaną bazę maili: Klienci → Importuj (CSV z kolumną `Accepts Email Marketing` = yes tylko przy zgodzie), potem instalacja Klaviyo (jeszcze nie zainstalowane), które zaciąga subskrybentów do listy „Newsletter”. Skrypt aktualizacji z arkusza: `tools/apply_catalog_v2.py` (zob. „Arkusz od Adriana naniesiony”); nadal nieobsłużone: nazwa_produktu (docelowe nazwy), kolory jako warianty, przeliczenie grup „inne kolory” po nazwie modelu, tag `wycofany` = ukryj.
- Skrypt importu pomija istniejące SKU, można uruchamiać ponownie; `--publish-only` publikuje nieopublikowane w Sklepie online.
- **Katalog PDF (08.09.2026):** `tools/build_catalog_pdf.py` (uruchamiać pythonem z Pillow, np. fotoenv) buduje z Admin API + zdjęć „rozjasnione” dwa PDF A4 (okładka ze spisem, 9 produktów na stronę, sekcje wg tagów): bez cen i z cenami netto; `--upload` / `--upload-only` wgrywa do Shopify → Pliki (cennik wgrywać pod losową nazwą `achti-katalog-b2b-cennik-<hex>.pdf` i po ponownym wgraniu zaktualizować adres). Adresy w ustawieniach motywu `b2b_catalog_pdf` / `b2b_catalog_pdf_prices` (grupa B2B), link w stopce (sections/footer.liquid, klucze `b2b.catalog_pdf*`): goście widzą wersję bez cen, firmy B2B z cenami. Po zmianach w katalogu (nazwy, ceny od Adriana) przebudować i wgrać ponownie. Kafelek produktu pokazuje SKU pod nazwą i badge „Dostępny od ręki” (snippets/card-product.liquid). Listing: 3 kolumny na desktopie.
- **Katalog v2 (08.09.2026):** sklep zsynchronizowany z folderem OneDrive „WYBRANE NA PLATFORME B2B Full Size” (317 zdjęć = 317 produktów, 47 starych usuniętych na stałe, demo „Barcelona” też). Zdjęcia po obróbce (`tools/foto_biale_tlo.py`: rembg isnet + miękka maska krawędzi bez poświaty, 2000×2500, białe tło): `~/Downloads/ACHTI B2B Full Size - biale tlo`; **w sklepie są wersje rozjaśnione** z `~/Downloads/ACHTI B2B Full Size - rozjasnione` (`tools/foto_rozjasnij.py auto` — gamma zależna od jasności czapki, ciemne mocniej, jasne prawie bez zmian). Skrypty: `tools/build_catalog_v2.py` (nazwy plików → `tools/katalog-v2.json` + arkusz `~/Downloads/achti-katalog-v2.csv/.xlsx` w układzie kolumn klienta + `waga_g`), `tools/sync_catalog_v2.py` (dopasowanie po starej nazwie pliku, podmiana zdjęć — pomija produkty z obrazem 2000 px, więc można uruchamiać ponownie; `--dry-run`, `--only=photos,data,create,delete,groups`). Ten sam kod na kilku zdjęciach = osobne produkty z sufiksem SKU: AZ-2033PC-TURBO, AZ-2842PC-BOY/-GIRL, AZ-910PC-BP (tytuł „(Turbo)”/„(bez pompona)”), AZ-607PCV→AZ-607PCV1; powiązane w `inne_kolory`. 6 modeli przeniesionych do dziecięcych (52-54/50-52), AZ-1430PC → Lambslook, AZ-3101PC nowy (Łódź). Tłumaczenia produktów EN/FR/DE wgrane ponownie. Arkusz v2 dla Adriana do uzupełnienia: nazwy, kolory, ceny, skład, Płeć, waga_g (per model).
- **Arkusz od Adriana naniesiony (10.09.2026, dwa przebiegi):** źródło = `achti-katalog-v2.xlsx` na Google Drive (folder „WORKSPACE ACHTI x KAMIL DZIEDZIC”; konektor Drive działa, ale plik xlsx pobierać przez Chrome i kopiować z ~/Downloads Finderem/osascript — sandbox nie czyta Downloads, a base64 z konektora przepisany ręcznie się psuje). Kopia ostatniego stanu: `tools/achti-katalog-v2-drive.xlsx`. Układ Adriana: `typ` = „Czapka zimowa”/„Opaska zimowa”, `podszycie` (4 wartości), `material` = skład %, `Cena netto`, `Płeć` = Ona / On / Unisex / „Dla dzieci Dziewczynka|Chłopczyk|Unisex”, `nazwa_produktu` = imię modelu (317 unikalnych, np. Adela, „Annie Mini” dla dzieci), `opis` (3 własne teksty). Skrypt `tools/apply_catalog_v2.py --xlsx=… [--dry-run]` (arkusz = źródło prawdy, dopasowanie po SKU, przy zmianie kodu po nazwie pliku zdjęcia + aktualizacja SKU: AZ-3038→AZ-3038PC, AZ-3057PC→AZ-3057LPC): tytuły „Czapka zimowa damska Adela” / męska / unisex / dziecięca / chłopięca / dziewczęca / „Opaska zimowa damska Sabine” (`tools/catalog_text.py`; nazwy miast i „Beanie” zniknęły z tytułów, handle produktów zostały stare), opisy z szablonu albo z kolumny opis (pierwsze zdanie = akapit wstępny) + cechy i specyfikacja (skład, podszycie, rozmiar, kod), tagi płci wg Płeć (rozmiar 50-52/52-54 = `dzieci` nawet przy „Ona”: AZ-2202PC, AZ-2515PC, AZ-2876PC — do potwierdzenia), tag `opaski` (36 opasek; kolekcji „Opaski” jeszcze nie ma — założyć w panelu po tagu), metapole `custom.sklad` (AZ-2932D „GRASS + VEZUV” bez składu), ceny netto wszystkich 317 kodów (tag `cena-do-uzupelnienia` już nie istnieje; `set_prices.py` jest nieaktualny). Aktualizuje `tools/achti-produkty.json` (name, segment, product_type, flags, sklad, podszycie, opis, price); tłumaczenia: `translate_catalog.py` (segmenty Męska/Unisex, opaski, skład przez słownik włókien, podszycie; własne opisy Adriana tłumaczone ręcznie w `tools/translations/opis-overrides.json` — przy nowym opisie w arkuszu dopisać tam EN/FR/DE, inaczej skrypt użyje szablonu i ostrzeże) + `set_translations.py en fr de --only=products` (metapola sklad/rozmiar tłumaczone po wartości PL, rich text). **Nie naniesione:** `kolory (po przecinku)` (warianty kolorów do decyzji), `waga_g` (puste), grupy `inne_kolory` zostały z CLIP (nazwy są unikalne, więc nie definiują grup). Tag `premium` (kategoria „Premium Merino”) nadany 10.09 dziesięciu modelom ze składem „wełna merino” (AZ-2928…2938, AZ-3053PC…3056PC) — apply_catalog_v2 zachowuje ten tag. Kolejna edycja arkusza: dry-run → live → translate_catalog → set_translations; nie używać `sync_catalog_v2.py --only=data` (przywróciłby tytuły robocze). Po zmianie cen przebudować katalog PDF z cenami (skrypt potrzebuje zdjęć z ~/Downloads — poza sandboxem).

## Znaczki składu: wełna i merino (11.09.2026, życzenie klienta)
- Tagi `welna` (skład zawiera wełnę, także merino — 31 modeli) i `merino` (wełna merino — 10 modeli) nadaje `tools/set_fibre_tags.py [--dry-run]` z metapola `custom.sklad` (rich text, regex wełn/wool/laine/wolle i merino). Po zmianie składów w arkuszu uruchomić ponownie; `apply_catalog_v2.py` te tagi zachowuje.
- `snippets/b2b-fibre-badge.liquid` (parametry `product`, `variant: card|product`): pigułka z ikoną — owca dla merino („Wełna merino”), kłębek dla wełny („Z wełną”); merino ma pierwszeństwo. Renderowana na kafelku (snippets/card-product.liquid, oba warianty karty, lewy górny róg, `.fibre-badge--card`) i pod tytułem na karcie produktu (blok `title` w sections/main-product.liquid). Teksty `b2b.fibre.*` w 4 locales, CSS blok „Znaczek składu” w b2b.css.

## Zdjęcia „na modelce” z AI (Gemini / nano banana, 11.09.2026)
- Klucz API: `~/.config/achti/gemini_key` (AI Studio, konto kamil.dziedzic@spoton.c…, klucz „Achti” …CZYA, projekt „Default Gemini Project” `gen-lang-client-0310151608`). **11.09.2026 projekt jest na Free tier i dzienny limit modeli obrazowych jest wyczerpany (429 RESOURCE_EXHAUSTED)** — do pracy wsadowej trzeba włączyć płatności: AI Studio → API Keys → wiersz „Achti” → „Set up billing”. Po włączeniu nic w skryptach się nie zmienia.
- Modele i ceny za zdjęcie (cennik Google, 11.09.2026): `gemini-3-pro-image` (Nano Banana Pro) 0,134 $ w 1K/2K i 0,24 $ w 4K; `gemini-3.1-flash-image` 0,067 $ (1K) / 0,101 $ (2K); `gemini-2.5-flash-image` (pierwsza nano banana) 0,039 $. Katalog ma 317 produktów (266 damskich, 24 męskie, 27 dziecięcych, w tym 36 opasek) → cała seria dorosłych na Pro ≈ 39 $.
- `tools/gen_model_photos.py`: `refs` (wzorce twarzy z opisów w `PERSONAS`), `batch` (packshot + wzorzec → zdjęcie na modelce, wątki, pomija już wygenerowane, `--codes/--limit/--persona/--force`), `preview` (strona HTML packshot obok wygenerowanego), `hat` (pojedyncze). Flagi `--model=`, `--size=`, `--workers=`, `--dry-run`. Katalogi: `~/Claude/achti-foto/modelki/{refs,out}`, packshoty z `~/Claude/achti-foto/wyrownane`. Prompt wymusza wierne odwzorowanie splotu, kolorów, pompona i metki oraz białe tło 4:5.
- Segmenty → persony w `SEGMENT_PERSONA` (damskie/unisex → kobieta, męskie → mężczyzna). **Dzieci pomijane domyślnie** (fotorealistyczne wizerunki dzieci: polityka Google i kwestia zgód) — do decyzji Adriana, na razie dziecięce zostają z packshotami.
- Casting 11.09.2026: `casting` generuje po 2 ujęcia na osobę (drugie z pierwszym jako wzorcem twarzy, więc to ta sama osoba) — 6 kobiet 22-25 (k1-k6), 4 mężczyzn (m1-m4), 3 dziewczynki (d1-d3), 3 chłopców (c1-c3), czapki AZ-3101PC (dorośli), AZ-2364PC (dziewczynki), AZ-2713PC (chłopcy). Sceny mieszane: Rynek, Kazimierz, Zakopane, góry, jesienny park i las. Wyniki: `~/Claude/achti-foto/modelki/casting` + kopia w `~/Downloads/achti-casting-modeli`. Czeka na wybór obsady przez Kamila/Adriana.
- `styl` (STYLE: kanapa/mur/kawiarnia/las) = ujęcia lookbookowe jak u Barts: cała sylwetka od pasa, stylizacja (płaszcz, szal, sweter), wzrok w obiektyw, chesterfield w plenerze / ceglany mur / kawiarnia / jesienny las. Bierze packshot + zdjęcie osoby z castingu. 5 sztuk pokazowych w `~/Downloads/achti-style-zdjec`.
- `oldmoney` (OLD_MONEY: dwor/stajnia/auto/ogrod/aleja) = cicha elegancja wg życzenia Adriana (11.09.2026): dwór z bluszczem, stajnia z koniem, vintage Land Rover, ogród strzyżony, aleja dębów z golden retrieverem; kaszmirowe płaszcze camel/granat/kość słoniowa, skórzane rękawiczki, zero logotypów, stonowany grading. Renderowane w 4K (`--size=4K`, 0,24 $/zdj.). 5 sztuk w `~/Downloads/achti-oldmoney`.
- **Bez blaszek (decyzja Kamila 11.09.2026):** prompt `oldmoney`/`seria` każe pominąć metalową metkę i pokazać czystą dzianinę — model nie potrafi napisać „ACHTI” w tej skali, a podmiana pikselowa zostawiała ślad. `tools/podmien_metke.py` zostaje w repo na wypadek powrotu do metek.
- `seria --damskie=… --meskie=… --dziewczece=… --chlopiece=…` generuje serię old money: jedna twarz na segment (`SERIA_OBSADA`: k4-braz, m2, d1, c1 — do zmiany po wyborze obsady), rotacja 5 scen (`SERIA_SCENY`), stylizacja dziecięca nadpisywana (dziewczynki: kremowy płaszczyk; chłopcy: granatowa budrysówka, sztruksy) — bez tego chłopcy wychodzili w kremowych płaszczach z apaszką. Pierwsza partia 15 zdjęć 4K: `~/Downloads/achti-oldmoney-seria`.
- **Metka ACHTI:** model obrazowy nie potrafi napisać tak drobnego tekstu (wychodzi „AORITI”, „ACHΣΕ”) — także w drugim przejściu (`metka`). Rozwiązanie: `tools/podmien_metke.py --foto=<png> [--code=] [--debug]` wycina prawdziwą blaszkę z packshotu i wkleja ją w wykryte miejsce (bounding box z `gemini-3.1-pro-preview`, dopasowanie koloru, miękka maska; obszar wklejki powiększony o 20/60 %, żeby zakryć „duchy” liter). Wymaga pythona z Pillow (venv fotoenv). Koszt detekcji ~0,01 zł. Zostaje delikatny ślad dzianiny wokół blaszki, widoczny dopiero przy 100 % zoomu w 4K.
- **Uwaga o sandboxie:** skrypt uruchamiany w tle (`&`) nie czyta `~/Claude/achti-foto` (widzi „brak packshotu”) — uruchamiać sekwencyjnie na pierwszym planie.
- Konto rozliczeniowe: Gemini płaci konto `kamildziedzic1337@gmail.com` (prepaid); konto spoton nie ma billingu. Przy wyczerpaniu kredytów API zwraca 429 „Your prepayment credits are depleted”. 11.09.2026 zużyto ~62 zdjęcia ≈ 8,4 $.
- Kolejność prac: włączyć płatności → `refs` → wybór twarzy przez Kamila/Adriana → `batch --limit=10` na próbę → ocena w `preview` → pełny przebieg → wgranie do Shopify jako drugie zdjęcie produktu (skrypt do dopisania).

## Inne kolory modelu (kółka na karcie produktu, 07.09.2026)
- Metapole `custom.inne_kolory` (list.product_reference, dostęp storefront PUBLIC_READ) = produkty tego samego modelu w innych kolorach. Blok `model_colors` w main-product renderuje bieżący + powiązane jako okrągłe miniatury (CSS `.model-colors` w b2b.css). Tabela szybkiego zamawiania (snippets/quick-order-list.liquid) pokazuje wiersze wszystkich produktów grupy z sumą dla grupy (przy produktach z wariantami dodaje wiersz nagłówkowy z nazwą). Kliknięcie w kółko podmienia produkt bez przeładowania (assets/model-colors.js: Section Rendering API dla sekcji `__main` i `__quick-order-list`, history.pushState; wstecz = reload). Klient edytuje listę w panelu przy produkcie. Demo: Milano ↔ Warmia ↔ Kujawy. Grupowanie zrobione 07.09.2026: CLIP ViT-B/32 (obrazy w skali szarości) + klasteryzacja aglomeracyjna (cosinus, próg 0.06) → 59 grup, 147 produktów; zapisane skryptem `tools/set_model_groups.py` z `tools/model-groups.json`. Podgląd dla klienta: `~/Downloads/achti-grupy-modeli.html` (+CSV). Poprawki: w panelu przy produkcie albo edycja JSON i ponowne uruchomienie skryptu (`--clear` czyści).

## Audyt 07.09.2026 — otwarte rekomendacje
- **Baner hero (11.09.2026): zdjęcie AI** „hero-para” (para przed dworem, styl old money) wygenerowane `gen_model_photos.py hero --size=4K`, przeskalowane do 3840 px JPEG i wgrane do Shopify → Pliki jako `achti-hero-para.jpg`; `templates/index.json` → `image_banner.settings.image = shopify://shop_images/achti-hero-para.jpg` (poprzednie: `IMG_1871_wynik.jpg`, kopia szablonu w /tmp/index.json.bak). Prompt `PROMPT_HERO` trzyma ludzi w lewej połowie, prawą trzecią pustą pod nagłówek i wolny dół na środku pod przycisk. Wideo: dostępne `veo-3.1-*` (1080p: 0,40 / 0,12 / 0,08 $ za sekundę), ale sekcja image-banner w Trade przyjmuje tylko obraz — pod wideo trzeba dołożyć tło wideo w sekcji.
- Hero na stronie głównej (10.09.2026 wieczorem): nagłówek „Czapki, kominy i kominiarki dla Twojego sklepu” w prawej trzeciej części zdjęcia na tle budynku (pudełko hero = siatka 3 kolumn na całą szerokość, nagłówek w 3. kolumnie przy prawej krawędzi, CSS w b2b.css blok „Hero na stronie głównej”, ≥990 px), przycisk „Sprawdź ofertę” od 13.09.2026 pod nagłówkiem w tej samej 3. kolumnie (`.banner__buttons` grid-column 3, justify-self end, max 40 rem, `justify-content: flex-start`, przycisk naturalnej szerokości; wcześniej bottom-center), nagłówek 3.8 rem / line-height 1.1, row-gap 2.4 rem, `.banner__content` padding-bottom 6 rem; podtytuł usunięty — tekst nie może zasłaniać modelek. Napis hero od 11.09.2026 czcionką treści (Jost), nie Tenor Sans — na życzenie Kamila.
- Strona główna: Nowości = kolekcja `nowosci` w trybie taśmy na całą szerokość ekranu (ustawienia sekcji `marquee` + `full_width`; przy marquee slider-component nie dostaje klasy page-width: produkty jadą w lewo, przycisk „Rozwiń wszystkie” zatrzymuje i pokazuje siatkę; assets/fc-marquee.js + CSS w b2b.css, 07.09.2026; od 10.09.2026 pozycję taśmy prowadzi JS przez requestAnimationFrame zamiast @keyframes, więc da się ją przeciągać palcem i myszką (pointer events, `touch-action: pan-y`, po przeciągnięciu połknięty klik), pauza po najechaniu; od 11.09.2026 po puszczeniu palca taśma jedzie wybiegiem (prędkość liczona z ostatnich ruchów, maks. 4000 px/s, wytracanie 94%/s), transform ustawiany raz na klatkę zamiast przy każdym pointermove, `will-change: transform`, prędkość bazowa 95 px/s (ustawienie `marquee_speed` w templates/index.json) i pauza poza ekranem przez IntersectionObserver. Od 10.09 sekcja Nowości to pas w beżu (`color_scheme: scheme-3` w templates/index.json; tło scheme-3: 11.09.2026 rano wróciło do #c2b7ac — „wersja A” na życzenie klienta; 10.09 wieczorem było #e0dbd6, wariant 4), nagłówek z podpisem po prawej i cienką linią (klasa `collection--marquee` w sections/featured-collection.liquid, CSS w b2b.css), równe kafelki (karta 100% wysokości, 2 linie na tytuł), hero ma gradient smoothstep tylko u góry w biel nagłówka (`.banner__media::before`; dolne przejście usunięte 11.09), przyklejony nagłówek ma pod sobą 6 rem zanikania bieli 100%→0 (smoothstep) z lekkim rozmyciem 6 px, widoczne dopiero po przewinięciu (klasa `header--scrolled` nadawana skryptem w sections/header.liquid przy scrollY > 24) (`.header-wrapper::after`: backdrop-filter blur + mask-image smoothstep, bez bielenia — biała mgiełka gryzła się z beżem), a wszystkie kafelki produktów (`.product-card-wrapper .card--card`: listing, wyszukiwarka, Bestsellery, polecane, taśma) są kryjąco jasne (#e8e3de od 11.09.2026 rano — „wersja A” dla klienta; 10.09 był wariant D #f8f6f5) ze zdjęciem w `mix-blend-mode: multiply` na `.card__media` (biel zdjęcia = kolor kafelka; `.card` ma transform, więc tło musi być na `.card`, a kontenery zdjęcia bez z-index). Uwaga: tag `nowosc` zniknął przy synchronizacji katalogu 08–10.09 i sekcja była pusta — 10.09 nadany ponownie 12 najwyższym kodom AZ-3087D–AZ-3102PC jako wersja poglądowa; docelowo Adrian taguje nowo dodane modele), Bestsellery = `najpopularniejsze` (automatyczna, wg sprzedaży). Karta produktu na dole: „Inni klienci też kupili” (related-products) + „Najbardziej popularne” (featured-collection `najpopularniejsze`). Animacja reveal-on-scroll wyłączona (`animations_reveal_on_scroll: false`) — wyglądała jak migotanie przy ładowaniu siatki.
- 9 linków ze stopki prowadzi do nieistniejących stron/polityk (O nas, Produkcja, Materiały, Jakość, Zrównoważony rozwój, Warunki współpracy, FAQ, Wysyłka, Zwroty) — treści klienta.
- Zdjęcia 1024 px w źródle: na karcie produktu (kolumna ~430 px) OK, w lightboxie miękkie; przy nowych zdjęciach z aparatu przepuścić przez `tools`/obróbkę tła.
- Animacja „reveal on scroll” (ustawienie motywu) opóźnia pokazanie sekcji przy szybkim przewijaniu — rozważyć wyłączenie.
- Certyfikaty (multicolumn) bez tekstów alt — uzupełnić alt w Treść → Pliki.

## Do zrobienia (stan na 04.09.2026)
1. Weryfikacja w sklepie: nagłówek, karta produktu i katalog jako gość / jako firma B2B.
2. Konfiguracja panelu (klient/Kamil): metapola produktu, kolekcje Nowości/Bestsellery, filtry w Search & Discovery, konta firm i katalogi B2B, języki + Translate & Adapt, Klaviyo.
3. Aplikacje: ulubione, powiadomienie o dostępności, katalog PDF z cenami po zalogowaniu.
4. Faza 2: Baselinker, feed XML, cross-sell w koszyku, szablon potwierdzenia zamówienia.
